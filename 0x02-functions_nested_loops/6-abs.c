#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 *
 * Return: Value of integer as a positive number.
 */
int _abs(int n)
{
	int result;
	 if (n >= 0)
		 result = num;
	 else if (num < 0)
		 result = -1 * num;
	 return (result);
}
